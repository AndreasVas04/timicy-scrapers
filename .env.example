# Copy this file to .env and fill in your real values locally.
# .env is gitignored — never commit real credentials. Placeholders only here.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:6543/postgres
